#!/bin/bash

pomodoro_timer=30

if [[ $@ == *"--long"* ]]; then pomodoro_timer=45; fi

start-timer() {
  for (( i = $pomodoro_timer; i > 0; i--)); do
    for (( j = 59; j > 0; j--)); do
      xsetroot -name "$i:$j"
      sleep 1s
    done
  done
}

if [[ $1 = "start" ]]; then
  start-timer &
fi
